# over80 - print all lines longer than 80 characters

    $ over80 *.c
    ./diff-no-index.c:202:static void append_basename(struct strbuf *path, const char *dir, const char *file)
    ./parse-options.c:203:static int parse_short_opt(struct parse_opt_ctx_t *p, const struct option *options)
    ./tree-walk.c:25:static int decode_tree_entry(struct tree_desc *desc, const char *buf, unsigned long size, struct strbuf *err)
    ./tree-walk.c:54:static int init_tree_desc_internal(struct tree_desc *desc, const void *buffer, unsigned long size, struct strbuf *err)
